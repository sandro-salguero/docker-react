language: generic
sudo: required

services:
  - docker

before_install:
  - docker build -t sandrosalguero/docker-react -f Dockerfile.dev .

script:
  - docker run -e CI=true sandrosalguero/docker-react npm run test